# 2. Environment Setup

## Step 1 — Connect to the Environment

```bash
ssh cloud-user@bastion.<YOUR_ID>.opentlc.com
Verify GPU access:

```bash
Copy code
nvidia-smi
Verify Podman:

```bash
Copy code
podman --version
If either fails → call a facilitator.

